-- Oracle Database Schema for CRUD API
-- Run this script to create the application table

-- Create PRODUCTS table
CREATE TABLE PRODUCTS (
    ID NUMBER(10) GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(200) NOT NULL,
    DESCRIPTION VARCHAR2(1000),
    PRICE NUMBER(18,2) NOT NULL,
    STOCK_QUANTITY NUMBER(10) NOT NULL,
    IS_ACTIVE NUMBER(1) DEFAULT 1 NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP
);

-- Create index for faster queries
CREATE INDEX IDX_PRODUCTS_IS_ACTIVE ON PRODUCTS(IS_ACTIVE);
CREATE INDEX IDX_PRODUCTS_CREATED_AT ON PRODUCTS(CREATED_AT);

-- Insert sample data
INSERT INTO PRODUCTS (NAME, DESCRIPTION, PRICE, STOCK_QUANTITY) VALUES ('Laptop', 'High-performance laptop', 999.99, 50);
INSERT INTO PRODUCTS (NAME, DESCRIPTION, PRICE, STOCK_QUANTITY) VALUES ('Mouse', 'Wireless mouse', 29.99, 200);
INSERT INTO PRODUCTS (NAME, DESCRIPTION, PRICE, STOCK_QUANTITY) VALUES ('Keyboard', 'Mechanical keyboard', 79.99, 100);

COMMIT;
